enum StructureType {
  // 结构，由 media/plainText/content 组成
  structure = "STRUCTURE",
  // 内容，由 unit 组成
  content = "CONTENT",
  // 多媒体
  media = "MEDIA",
  // 纯文本
  plainText = "PLAINTEXT",
  // 不可分割的最小单元
  unit = "UNIT",
}

export default StructureType;
